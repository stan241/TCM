{
  "name": "@tcm/db",
  "version": "0.1.0",
  "private": true,
  "description": "TCM database migrations and scripts",
  "scripts": {
    "migrate":     "node scripts/migrate.js",
    "migrate:dry": "node scripts/migrate.js --dry-run",
    "healthcheck": "node scripts/healthcheck.js"
  },
  "dependencies": {
    "pg": "^8.11.0"
  }
}
